import {
  canonicalPerimeterNames,
  firstCircleStations,
  requireCanonicalSubstrate,
  verifyCanonicalSubstrate
} from './canonical-substrate.js';

const SVG_NS = 'http://www.w3.org/2000/svg';
const ORIGIN = Object.freeze({ x: 180, y: 150 });
const R = 90;
const VIEW_BOX = '0 0 360 300';

const UNIT_CHORDS = Object.freeze(['AB', 'BC', 'CD', 'DE', 'EF', 'FA']);
const NEXT_CHORDS = Object.freeze(['AC', 'BD', 'CE', 'DF', 'EA', 'FB']);
const DIAMETERS = Object.freeze(['AD', 'BE', 'CF']);
const CENTRE_FAN = Object.freeze([['O', 'A', 'B'], ['O', 'B', 'C'], ['O', 'C', 'D'], ['O', 'D', 'E'], ['O', 'E', 'F'], ['O', 'F', 'A']]);
const DEGENERATE = Object.freeze([['O', 'A', 'D'], ['O', 'B', 'E'], ['O', 'C', 'F']]);
const BOUNDARY_TRIPLES = Object.freeze([['A', 'B', 'C'], ['A', 'C', 'D'], ['A', 'C', 'E'], ['B', 'D', 'F'], ['B', 'F', 'E']]);
const BFE = Object.freeze(['B', 'F', 'E']);
const CARRIER_GROUPS = Object.freeze([['AO', 'OD', 'AD'], ['BO', 'OE', 'BE'], ['CO', 'OF', 'CF']]);
const PARALLEL_FAMILIES = Object.freeze([['AB', 'CF', 'DE'], ['BC', 'AD', 'EF'], ['CD', 'BE', 'FA']]);

function el(name, attrs = {}, children = []) {
  const node = document.createElementNS(SVG_NS, name);
  for (const [key, value] of Object.entries(attrs)) if (value !== undefined && value !== null) node.setAttribute(key, String(value));
  for (const child of children) node.appendChild(child);
  return node;
}
function text(x, y, value, className = 'tz-label') { const node = el('text', { x, y, class: className }); node.textContent = value; return node; }
function diagnosticSvg(title, verification) { const svg = el('svg', { viewBox: VIEW_BOX, role: 'img', class: 'substrate-svg substrate-diagnostic', 'aria-label': `${title} diagnostic` }); svg.appendChild(el('rect', { x: 12, y: 12, width: 336, height: 276, rx: 18, class: 'tz-frame' })); svg.appendChild(text(22, 38, 'SUBSTRATE CONTRACT FAILED', 'tz-title')); svg.appendChild(text(22, 62, 'Renderer failed closed; no geometry drawn.', 'tz-subtitle')); const message = verification?.failures?.[0] ? `${verification.failures[0].label} mismatch` : 'canonical verification did not pass'; svg.appendChild(text(22, 86, message, 'tz-caption')); return svg; }
function substratePoints() { const verification = verifyCanonicalSubstrate(); if (!verification.ok) return null; requireCanonicalSubstrate(); return firstCircleStations({ origin: ORIGIN, radius: R }); }
function withVerifiedSubstrate(title, render) { const verification = verifyCanonicalSubstrate(); if (!verification.ok) return diagnosticSvg(title, verification); const points = substratePoints(); if (!points) return diagnosticSvg(title, verification); return render(points); }
function baseSvg(title) { const svg = el('svg', { viewBox: VIEW_BOX, role: 'img', class: 'substrate-svg', 'aria-label': title }); svg.appendChild(el('rect', { x: 12, y: 12, width: 336, height: 276, rx: 18, class: 'tz-frame' })); return svg; }
function perimeterClosed() { const names = canonicalPerimeterNames(); return [...names, names[0]]; }
function stationNames() { return ['O', ...canonicalPerimeterNames()]; }
function pairNames(pair) { return pair.split(''); }
function line(a, b, className = 'tz-line') { return el('line', { x1: a.x, y1: a.y, x2: b.x, y2: b.y, class: className }); }
function pairLine(points, pair, className = 'tz-line') { const [a, b] = pairNames(pair); return line(points[a], points[b], className); }
function appendPairSet(svg, points, pairs, className) { pairs.forEach((pair) => svg.appendChild(pairLine(points, pair, className))); }
function extendedLine(a, b, className = 'tz-carrier') { const dx = b.x - a.x; const dy = b.y - a.y; const length = Math.hypot(dx, dy) || 1; const reach = 260; const ux = dx / length; const uy = dy / length; return el('line', { x1: ORIGIN.x - ux * reach, y1: ORIGIN.y - uy * reach, x2: ORIGIN.x + ux * reach, y2: ORIGIN.y + uy * reach, class: className }); }
function polygon(points, stationList, className = 'tz-fill') { return el('polygon', { points: stationList.map((name) => `${points[name].x},${points[name].y}`).join(' '), class: className }); }
function point(points, name, className = 'tz-point') { const p = points[name]; const dx = name === 'A' || name === 'D' ? -18 : 8; const dy = name === 'E' || name === 'F' ? 18 : -8; return el('g', {}, [el('circle', { cx: p.x, cy: p.y, r: 4.5, class: className }), text(p.x + dx, p.y + dy, p.label)]); }
function circle(points, className = 'tz-circle') { return el('circle', { cx: points.O.x, cy: points.O.y, r: R, class: className }); }
function arcPath(points, stationList) { let d = `M ${points[stationList[0]].x} ${points[stationList[0]].y}`; for (let i = 1; i < stationList.length; i += 1) d += ` L ${points[stationList[i]].x} ${points[stationList[i]].y}`; return d; }
function appendAllStations(svg, points, activeNames = []) { const active = new Set(activeNames); stationNames().forEach((name) => svg.appendChild(point(points, name, name === 'O' ? 'tz-point tz-origin' : active.has(name) ? 'tz-point tz-active' : 'tz-point'))); }
function appendBoundary(svg, points, className = 'tz-step') { const stationList = perimeterClosed(); for (let i = 0; i < stationList.length - 1; i += 1) svg.appendChild(line(points[stationList[i]], points[stationList[i + 1]], className)); }
function appendRadii(svg, points, className = 'tz-radius') { canonicalPerimeterNames().forEach((name) => svg.appendChild(line(points.O, points[name], className))); }
function appendDiameters(svg, points, className = 'tz-diameter') { appendPairSet(svg, points, DIAMETERS, className); }
function appendParallelFamilies(svg, points) { PARALLEL_FAMILIES.forEach((family, index) => appendPairSet(svg, points, family, `tz-carrier-segment tz-family-${index + 1}`)); }
function appendCarriers(svg, points, soft = false) { svg.appendChild(extendedLine(points.A, points.D, `tz-carrier tz-carrier-a${soft ? ' tz-soft' : ''}`)); svg.appendChild(extendedLine(points.B, points.E, `tz-carrier tz-carrier-b${soft ? ' tz-soft' : ''}`)); svg.appendChild(extendedLine(points.C, points.F, `tz-carrier tz-carrier-c${soft ? ' tz-soft' : ''}`)); }
function appendCarrierCollapse(svg, points) { appendCarriers(svg, points); CARRIER_GROUPS.forEach((group, index) => appendPairSet(svg, points, group, `tz-nested-segment tz-family-${index + 1}`)); }
function appendTriangleEdges(svg, points, stationList, className = 'tz-proof') { svg.appendChild(line(points[stationList[0]], points[stationList[1]], className)); svg.appendChild(line(points[stationList[1]], points[stationList[2]], className)); svg.appendChild(line(points[stationList[2]], points[stationList[0]], className)); }
function appendTriangleSet(svg, points, triples, fillClass = 'tz-fill', edgeClass = 'tz-proof') { triples.forEach((triple, index) => { svg.appendChild(polygon(points, triple, `${fillClass} ${index % 2 ? 'tz-cell-fill-b' : 'tz-cell-fill-a'}`)); appendTriangleEdges(svg, points, triple, edgeClass); }); }
function appendRightAngleMarker(svg, points, corner, a, b) { const p = points[corner]; const pa = points[a]; const pb = points[b]; const size = 14; const va = { x: pa.x - p.x, y: pa.y - p.y }; const vb = { x: pb.x - p.x, y: pb.y - p.y }; const la = Math.hypot(va.x, va.y) || 1; const lb = Math.hypot(vb.x, vb.y) || 1; const a1 = { x: p.x + (va.x / la) * size, y: p.y + (va.y / la) * size }; const b1 = { x: p.x + (vb.x / lb) * size, y: p.y + (vb.y / lb) * size }; const mid = { x: a1.x + (vb.x / lb) * size, y: a1.y + (vb.y / lb) * size }; svg.appendChild(el('path', { d: `M ${a1.x} ${a1.y} L ${mid.x} ${mid.y} L ${b1.x} ${b1.y}`, class: 'tz-angle-marker' })); }

function renderBasic(type) { return withVerifiedSubstrate(type, (points) => { const svg = baseSvg(type); svg.appendChild(circle(points, type === 'residue' ? 'tz-circle tz-fading' : 'tz-circle tz-muted')); if (type === 'sweep') { svg.appendChild(line(points.O, points.A, 'tz-radius tz-animate-draw')); svg.appendChild(circle(points, 'tz-circle tz-animate-circle')); } if (type === 'carry') { svg.appendChild(line(points.O, points.A, 'tz-radius')); svg.appendChild(line(points.A, points.B, 'tz-step tz-animate-draw')); } if (type === 'equilateral') appendTriangleSet(svg, points, [['O', 'A', 'B']], 'tz-fill', 'tz-proof'); if (type === 'closure') { appendBoundary(svg, points, 'tz-step'); svg.appendChild(el('path', { d: arcPath(points, perimeterClosed()), class: 'tz-walk tz-animate-draw' })); } if (type === 'residue') appendRadii(svg, points, 'tz-ghost-line'); appendAllStations(svg, points, type === 'carry' ? ['B'] : type === 'sweep' ? ['A'] : canonicalPerimeterNames()); return svg; }); }
function renderRadiiChords() { return withVerifiedSubstrate('radii chords', (points) => { const svg = baseSvg('radii chords'); svg.appendChild(circle(points, 'tz-circle tz-muted')); appendRadii(svg, points, 'tz-radius tz-role-radius'); appendBoundary(svg, points, 'tz-step tz-role-chord'); appendAllStations(svg, points, canonicalPerimeterNames()); return svg; }); }
function renderSixFan() { return withVerifiedSubstrate('six fan', (points) => { const svg = baseSvg('six fan'); svg.appendChild(circle(points, 'tz-circle tz-muted')); appendTriangleSet(svg, points, CENTRE_FAN, 'tz-fill', 'tz-proof'); appendAllStations(svg, points, canonicalPerimeterNames()); return svg; }); }
function renderDiameters() { return withVerifiedSubstrate('diameters', (points) => { const svg = baseSvg('diameters'); svg.appendChild(circle(points, 'tz-circle tz-muted')); appendDiameters(svg, points, 'tz-diameter tz-animate-draw'); appendAllStations(svg, points, canonicalPerimeterNames()); return svg; }); }
function renderCarriers() { return withVerifiedSubstrate('carriers', (points) => { const svg = baseSvg('carriers'); svg.appendChild(circle(points, 'tz-circle tz-muted')); appendCarriers(svg, points); appendAllStations(svg, points, canonicalPerimeterNames()); return svg; }); }
function renderInventory() { return withVerifiedSubstrate('inventory', (points) => { const svg = baseSvg('inventory'); svg.appendChild(circle(points, 'tz-circle tz-muted')); appendRadii(svg, points, 'tz-radius tz-role-radius'); appendBoundary(svg, points, 'tz-step tz-role-chord'); appendDiameters(svg, points, 'tz-diameter tz-soft'); appendParallelFamilies(svg, points); appendAllStations(svg, points, canonicalPerimeterNames()); return svg; }); }
function renderUnitPairs() { return withVerifiedSubstrate('unit pairs', (points) => { const svg = baseSvg('unit pairs'); svg.appendChild(circle(points, 'tz-circle tz-muted')); appendRadii(svg, points, 'tz-radius tz-role-radius'); appendPairSet(svg, points, UNIT_CHORDS, 'tz-step tz-role-chord'); appendAllStations(svg, points, canonicalPerimeterNames()); return svg; }); }
function renderLengthClasses() { return withVerifiedSubstrate('length classes', (points) => { const svg = baseSvg('length classes'); svg.appendChild(circle(points, 'tz-circle tz-muted')); appendPairSet(svg, points, UNIT_CHORDS, 'tz-unit-class'); appendRadii(svg, points, 'tz-unit-class'); appendPairSet(svg, points, NEXT_CHORDS, 'tz-next-class'); appendDiameters(svg, points, 'tz-diameter'); appendAllStations(svg, points, canonicalPerimeterNames()); return svg; }); }
function renderCarrierCollapse() { return withVerifiedSubstrate('carrier collapse', (points) => { const svg = baseSvg('carrier collapse'); svg.appendChild(circle(points, 'tz-circle tz-muted')); appendCarrierCollapse(svg, points); appendAllStations(svg, points, canonicalPerimeterNames()); return svg; }); }
function renderParallelism() { return withVerifiedSubstrate('parallelism', (points) => { const svg = baseSvg('parallelism'); svg.appendChild(circle(points, 'tz-circle tz-muted')); appendParallelFamilies(svg, points); appendCarriers(svg, points, true); appendAllStations(svg, points, canonicalPerimeterNames()); return svg; }); }
function renderSameness() { return withVerifiedSubstrate('sameness', (points) => { const svg = baseSvg('sameness'); svg.appendChild(circle(points, 'tz-circle tz-muted')); appendPairSet(svg, points, ['OA', 'AB'], 'tz-unit-class'); appendPairSet(svg, points, ['AO', 'AD'], 'tz-diameter'); appendPairSet(svg, points, ['AB', 'DE'], 'tz-carrier-segment tz-family-1'); appendPairSet(svg, points, ['BC', 'EF'], 'tz-carrier-segment tz-family-2'); appendAllStations(svg, points, canonicalPerimeterNames()); return svg; }); }
function renderTripleCatalogue() { return withVerifiedSubstrate('triple catalogue', (points) => { const svg = baseSvg('triple catalogue'); svg.appendChild(circle(points, 'tz-circle tz-muted')); DEGENERATE.forEach((triple) => appendTriangleEdges(svg, points, triple, 'tz-diameter tz-soft')); appendTriangleSet(svg, points, [['O', 'A', 'B'], ['A', 'C', 'E'], ['B', 'D', 'F']], 'tz-fill', 'tz-proof'); appendAllStations(svg, points, canonicalPerimeterNames()); return svg; }); }
function renderCentreTriples() { return withVerifiedSubstrate('centre triples', (points) => { const svg = baseSvg('centre triples'); svg.appendChild(circle(points, 'tz-circle tz-muted')); appendTriangleSet(svg, points, CENTRE_FAN, 'tz-fill', 'tz-proof'); appendDiameters(svg, points, 'tz-diameter tz-soft'); appendAllStations(svg, points, canonicalPerimeterNames()); return svg; }); }
function renderBoundaryTriples() { return withVerifiedSubstrate('boundary triples', (points) => { const svg = baseSvg('boundary triples'); svg.appendChild(circle(points, 'tz-circle tz-muted')); appendTriangleSet(svg, points, BOUNDARY_TRIPLES, 'tz-fill', 'tz-proof'); appendBoundary(svg, points, 'tz-ghost-line'); appendAllStations(svg, points, canonicalPerimeterNames()); return svg; }); }
function renderRightAngle() { return withVerifiedSubstrate('right angle', (points) => { const svg = baseSvg('right angle'); svg.appendChild(circle(points, 'tz-circle tz-muted')); svg.appendChild(polygon(points, BFE, 'tz-fill tz-cell-fill-a')); appendTriangleEdges(svg, points, BFE, 'tz-proof'); svg.appendChild(pairLine(points, 'BE', 'tz-diameter')); svg.appendChild(pairLine(points, 'FE', 'tz-unit-class')); svg.appendChild(pairLine(points, 'BF', 'tz-next-class')); appendRightAngleMarker(svg, points, 'F', 'B', 'E'); appendAllStations(svg, points, ['B', 'F', 'E']); return svg; }); }
function renderNextChords() { return withVerifiedSubstrate('next chords', (points) => { const svg = baseSvg('next chords'); svg.appendChild(circle(points, 'tz-circle tz-muted')); appendPairSet(svg, points, NEXT_CHORDS, 'tz-next-class'); appendAllStations(svg, points, canonicalPerimeterNames()); return svg; }); }
function renderProjection() { return withVerifiedSubstrate('projection', (points) => { const svg = baseSvg('projection'); svg.appendChild(circle(points, 'tz-circle tz-muted')); appendCarriers(svg, points, true); svg.appendChild(line(points.O, points.C, 'tz-radius')); svg.appendChild(line(points.C, points.D, 'tz-projection-leg')); svg.appendChild(line(points.O, points.D, 'tz-projection-leg tz-soft')); appendAllStations(svg, points, ['O', 'C', 'D']); return svg; }); }
function renderCoordinatePressure() { return withVerifiedSubstrate('coordinate pressure', (points) => { const svg = baseSvg('coordinate pressure'); svg.appendChild(circle(points, 'tz-circle tz-muted')); appendParallelFamilies(svg, points); appendCarriers(svg, points); appendRadii(svg, points, 'tz-ghost-line'); appendAllStations(svg, points, canonicalPerimeterNames()); return svg; }); }
function renderCoordinateRoute() { return withVerifiedSubstrate('coordinate route', (points) => { const svg = baseSvg('coordinate route'); svg.appendChild(circle(points, 'tz-circle tz-muted')); appendCarriers(svg, points, true); svg.appendChild(pairLine(points, 'OD', 'tz-route-step')); svg.appendChild(pairLine(points, 'DC', 'tz-route-step')); svg.appendChild(pairLine(points, 'OC', 'tz-next-class')); appendAllStations(svg, points, ['O', 'D', 'C']); return svg; }); }
function renderCascade() { return withVerifiedSubstrate('cascade', (points) => { const svg = baseSvg('cascade'); svg.appendChild(circle(points, 'tz-circle tz-muted')); appendRadii(svg, points, 'tz-ghost-line'); appendBoundary(svg, points, 'tz-step tz-soft'); appendPairSet(svg, points, NEXT_CHORDS, 'tz-next-class'); appendDiameters(svg, points, 'tz-diameter'); appendParallelFamilies(svg, points); appendAllStations(svg, points, canonicalPerimeterNames()); return svg; }); }
function renderPairIntro() { return withVerifiedSubstrate('pair intro', (points) => { const svg = baseSvg('pair intro'); svg.appendChild(circle(points, 'tz-circle tz-muted')); appendAllStations(svg, points, canonicalPerimeterNames()); svg.appendChild(pairLine(points, 'AB', 'tz-step tz-animate-draw')); return svg; }); }
function renderInspector() { return withVerifiedSubstrate('inspector', (points) => { const svg = baseSvg('inspector'); svg.appendChild(circle(points, 'tz-circle tz-muted')); appendPairSet(svg, points, ['OA', 'AB', 'AD', 'AC', 'BE', 'CF'], 'tz-inspector-line'); appendParallelFamilies(svg, points); appendAllStations(svg, points, canonicalPerimeterNames()); return svg; }); }

const RENDERERS = {};
function add(ids, renderer) { ids.forEach((id) => { RENDERERS[id] = renderer; }); }

add(['V001_first_radius_sweep'], () => renderBasic('sweep'));
add(['V002_carried_opening_to_B'], () => renderBasic('carry'));
add(['V003_forced_equilateral_OAB', 'V094_unit_equilateral_area_cell', 'V129_B_as_reason_intersection'], () => renderBasic('equilateral'));
add(['V004_six_step_boundary_closure'], () => renderBasic('closure'));
add(['V005_release_curve_seven_point_residue', 'V026_field_disclosure_closing', 'V127_circle_as_equality_locus', 'V158_locus_condition_OP_equals_1', 'V159_circle_near_equal_far_boundary'], () => renderBasic('residue'));
add(['V006_radii_chords_role_change', 'V136_fixed_opening_engine', 'V137_practical_rule_fixed_opening', 'V139_radius_to_chord_portability', 'V146_opening_as_coordinate_step', 'V151_proof_condition_portability', 'V152_chain_break_warning', 'V153_first_authority_opening', 'V154_first_transformation_fixed_opening', 'V155_invariance_before_name', 'V178_station_as_radius_endpoint', 'V223_same_length_different_role_opening', 'V224_same_length_inspector'], renderRadiiChords);
add(['V007_six_equilateral_fan', 'V092_recurrence_to_cells_transition', 'V093_cells_enter_field', 'V100_piece_accounting_area_proof', 'V104_sixth_turn_rotation', 'V162_circle_as_sixfold_selector', 'V163_cyclic_order_system', 'V169_circle_symmetry_display', 'V181_neighbour_role_unit_chords_cells', 'V258_equilateral_cell_proof_recurrence', 'V259_local_sixfold_neighbourhood_recurrence', 'V277_first_hexagon_as_assembled_body', 'V278_two_cell_rhombus_assembly', 'V279_boundary_vs_cell_reading', 'V282_accountable_decomposition', 'V301_cut_changes_reading_not_surface', 'V304_cell_grammar_of_parts', 'V325_recomposition_reveals_measure'], renderSixFan);
add(['V008_three_diameters_straight_angles', 'V165_circle_as_diameter_carrier'], renderDiameters);
add(['V009_primary_parallel_families'], renderCarriers);
add(['V010_local_inventory_plate'], renderInventory);
add(['V011_pair_catalogue_unit_classes', 'V148_unit_class_search', 'V257_unit_edge_recurrence'], renderUnitPairs);
add(['V012_pair_catalogue_length_classes', 'V030_role_count_closure_21', 'V037_pair_catalogue_full_summary', 'V056_pair_catalogue_length_inventory_locked', 'V164_circle_as_chord_generator'], renderLengthClasses);
add(['V013_segments_vs_carriers'], renderCarrierCollapse);
add(['V014_direction_vs_line_parallelism', 'V105_reflection_across_AD', 'V226_same_direction_parallelism', 'V260_direction_recurrence_carriers'], renderParallelism);
add(['V015_modes_of_sameness', 'V024_algebra_as_memory', 'V113_opening_role_metamorphosis', 'V130_station_role_wheel', 'V143_four_roles_one_length', 'V175_circle_to_station_roles_transition', 'V176_station_label_vs_role', 'V188_station_role_catalogue', 'V189_A_role_timeline', 'V218_identity_readings_on_residue', 'V222_what_kind_of_same_transition'], renderSameness);
add(['V016_triple_catalogue_degenerate_genuine', 'V042_triple_count_lock_35_3_32', 'V047_full_triple_catalogue_summary', 'V049_angle_from_triple_forcing', 'V212_alternating_family_identity'], renderTripleCatalogue);
add(['V017_centre_boundary_triples', 'V019_identity_vs_instance', 'V131_selection_activates_relation', 'V221_instance_identity_definition_plate'], renderCentreTriples);
add(['V018_boundary_triangle_gap_catalogue', 'V040_pair_to_triple_transition', 'V045_boundary_gap_family_examples', 'V046_boundary_catalogue_count_lock', 'V048_triples_generate_angle_families', 'V060_boundary_triangle_length_angle_catalogue', 'V061_boundary_catalogue_complete_ledger', 'V132_triangle_catalogue_as_identity_lens', 'V197_selected_triple_family_examples', 'V210_boundary_instances_to_families', 'V213_consecutive_family_identity', 'V262_triangle_identity_recurrence', 'V296_larger_triangle_decomposition_options'], renderBoundaryTriples);
add(['V020_right_angle_root_three_lock', 'V022_ratio_pattern_30_60_90', 'V050_right_angle_family_pending', 'V052_triple_catalogue_to_right_angle_transition', 'V053_BFE_proof_setup', 'V054_diameter_forces_right_angle', 'V057_BFE_ratio_pattern', 'V058_ratio_before_trig_names', 'V063_similarity_question_transition', 'V065_ratio_comparison_callouts', 'V069_trig_names_from_ratios', 'V070_BFE_trig_values', 'V071_trig_dependency_chain', 'V095_unit_cell_area_derivation', 'V168_instance_vs_family_on_circle', 'V182_opposite_role_diameters', 'V211_diameter_standing_identity', 'V227_same_angle_pattern', 'V305_right_triangle_as_half_cell'], renderRightAngle);
add(['V021_notice_vs_use_root_three'], renderNextChords);
add(['V023_native_coordinate_pressure', 'V033_recurrent_direction_field_pressure', 'V076_negative_native_steps', 'V081_basis_relative_coordinates', 'V082_coordinates_to_ring_growth_transition', 'V084_repeatable_centre_function', 'V085_ring_shell_growth_6n', 'V086_hexagonal_number_count', 'V089_exact_not_equal_angle', 'V106_translation_across_field', 'V170_circle_as_local_origin', 'V171_circle_vs_field_distinction', 'V202_field_selection_opportunities', 'V220_identity_recurrence_in_field', 'V245_recurrence_invariant_condition', 'V247_algebraic_search_invariant_rule', 'V253_recurrence_as_governed_change', 'V255_first_circle_conditions_recur', 'V256_seed_catalogue_vs_totality', 'V261_distance_class_field_search', 'V265_first_circle_as_local_model'], renderCoordinatePressure);
add(['V025_relation_cascade_summary', 'V038_algebra_waiting_as_memory', 'V039_pair_to_field_chain', 'V059_dependency_chain_to_trig', 'V062_cascade_to_ratio_pressure', 'V091_portable_relation_seed_catalogue', 'V111_governed_change_grammar', 'V112_invariant_closure', 'V121_earned_catalogue_map', 'V122_return_with_knowledge_transition', 'V123_ledger_recap_lens_opening', 'V124_return_not_restart', 'V133_field_as_recurrence_place', 'V173_circle_reading_dependency_chain', 'V174_same_curve_many_lenses', 'V204_select_inspect_classify_lock_reinspect', 'V206_selection_catalogue_reinspection', 'V207_selection_to_identity_transition', 'V208_selection_to_identity_question', 'V252_invariant_to_field_reinspection_transition', 'V275_identity_assembly_into_bodies', 'V284_assembly_to_recomposition_chain', 'V302_piece_account_checklist', 'V324_field_supports_recomposition_sequence', 'V327_lawful_recomposition_definition', 'V328_reinspection_lens_synthesis', 'V329_part_two_reinspection_frame', 'V344_reinspection_method_loop', 'V346_part_three_discipline_gate', 'V347_part_two_teaching_chain'], renderCascade);
add(['V027_pair_as_relation'], renderPairIntro);
add(['V034_join_attribute_inspector', 'V036_invariance_not_appearance', 'V066_similarity_scaling_family', 'V067_scale_preserves_proportion', 'V068_congruence_vs_similarity', 'V090_four_field_search_modes', 'V101_area_to_transformation_transition', 'V102_first_invariant_governed_change', 'V103_transformation_grammar_overview', 'V109_transformation_invariant_ledger', 'V110_sameness_requires_invariant', 'V125_fact_as_lens_protocol', 'V135_invariant_as_lens', 'V183_station_as_selection_participant', 'V190_identity_survives_use', 'V191_station_roles_to_selection_transition', 'V193_available_vs_active_relation', 'V194_pair_selection_role_switch', 'V200_transformation_identity_survival', 'V201_difference_then_invariant', 'V203_selection_not_proof_warning', 'V205_selection_does_not_prove', 'V216_identity_qualified_by_invariant', 'V216_scaling_separates_identity_readings', 'V217_same_requires_reading', 'V233_resemblance_vs_invariant', 'V238_change_requires_invariant_question', 'V249_expanded_transformation_ledger', 'V250_three_questions_for_change', 'V251_invariant_makes_change_mathematical', 'V276_lawful_assembly_not_arbitrary_drawing', 'V280_body_sameness_invariants', 'V281_lawful_cuts_decomposition', 'V297_decomposition_disciplined', 'V298_lawful_vs_arbitrary_cutting', 'V299_cut_availability_rule', 'V303_diagram_vs_piece_accounting', 'V306_distinct_routes_warning', 'V323_recomposition_invariant_profile', 'V326_piece_account_proof_transfer', 'V343_enrichment_without_mythology', 'V345_method_protects_from_drift', 'V348_part_two_closing_aphorism'], renderInspector);
add(['V072_unit_radius_projection_reading', 'V073_ratio_to_coordinate_transition'], renderProjection);
add(['V075_native_basis_uv', 'V077_route_vs_straight_distance', 'V078_native_distance_derivation', 'V079_distance_rule_test_points', 'V080_thirty_degree_ray_signature', 'V083_route_distance_memory', 'V087_shell_route_distance_distinction', 'V128_A_as_station_centre_candidate', 'V186_station_coordinate_memory_role', 'V246_coordinate_movement_invariant', 'V263_coordinates_as_recurrence_memory'], renderCoordinateRoute);

const ACTIVE_VISUAL_IDS = new Set(Object.keys(RENDERERS));
export function hasSubstrateRenderer(visualId) { return ACTIVE_VISUAL_IDS.has(visualId); }
export function renderSubstrateVisual(visualId) { const renderer = RENDERERS[visualId]; return renderer ? renderer() : null; }
